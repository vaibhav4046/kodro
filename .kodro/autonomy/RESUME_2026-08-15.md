# Resume point, 15 August 2026

Written at the owner's instruction to stop mid-pass. Everything needed to pick
up without re-deriving it is here. Nothing in this file is a claim about work
that was finished: the "open" section is open.

## Git state at the stop

- Branch `agent/kodro-ca2-candidate`, HEAD `ced148d`. Nothing committed during
  the last two passes.
- Eight files carry uncommitted edits, all intentional, none half-applied:
  - `.kodro/autonomy/STATE.md`
  - `.kodro/ca2-evidence/2026-08-15-bcs-citations-and-aux-shadowing.md`
  - `docs/HANDOFF_GPT56_COMPLETE.md`
  - `docs/HANDOFF_MASTER_PROMPT.md`
  - `docs/dissertation/CA2_INTEGRITY_AUDIT_2026-08-14.md`
  - `scripts/qa_citations.py`
  - `scripts/qa_citations_allow.txt`
  - `src/kodro/web/app.py`
- Three stashes exist and must stay untouched. `docs/dissertation/_build/`
  holds 107 tracked files and must never be removed. `dist/` is gitignored but
  holds the owner's built exes.

## What the last two passes actually established

1. The waivable-hard-failure change in `scripts/qa_citations.py` is verified
   working AND verified not to have removed the check. A staged document with
   three unwaived bad citations produced three FAIL lines and `SELFTEST_RC=1`.
   Three rather than two matters: it proves the continuation-citation branch
   reaches the past-end check, not only the resolve check. Current green run is
   168 citations across 107 documents, 0 unresolvable, 0 needing review, 44
   waived, 114 backticked `:N` that are not citations, exit 0.
2. All six scripts that a throwaway scanner called undocumented have real
   callers. Zero defects there. The scanner had a third blind spot: it required
   a `scripts/` prefix, so files named by bare basename read as missing.
3. `docs/HANDOFF_MASTER_PROMPT.md` named `docs/dissertation/Kodro_Dissertation.html`
   as the dissertation source at two sites, one of them a work instruction a
   future agent would follow. The file was added in `0790128` and deleted in
   `6225db1`. Both sites now name the LaTeX source. The `.tex` is self-contained:
   0 `\input{`, 0 `\include{`, 0 `\subfile{`, counted with literal `-F`.
4. `src/kodro/web/app.py` shipped a module docstring crediting a vendored
   Claude design and claiming grading was a planned follow-up, while
   `submit_attempt` in the same file grades against `success_criteria` and
   returns hints. Rewritten to describe what the module does. Module parses; no
   test depended on the old text.
5. A repo-wide path sweep over every backticked path in every tracked Markdown
   file: 195 distinct paths named, 6 absent, 3 untracked but on disk. Every one
   read. All ten are correct statements or gitignored artefacts.

## The one item left open at the stop, now closed

At the stop, a design-track file that no longer exists was cited at two live
sites. The history is resolved and must not be re-guessed:

    a24a7c1  A   docs/design/REQUESTS_FOR_CLAUDE.md  (and REQUESTS_FOR_CODEX.md)
    186a675  D   both, and A docs/design/REQUESTS.md
    02b2c97  A   docs/design/REQUESTS_FOR_CLAUDE.md  (re-added)
    bbf4c81  R100 docs/design/REQUESTS_FOR_CLAUDE.md -> docs/design/REQUESTS_FROM_DESIGN.md

R100 means the rename was byte-identical. The successor is
`docs/design/REQUESTS_FROM_DESIGN.md`, not `docs/design/REQUESTS.md`. The two
dangling pointers are leftovers from that rename:

- `docs/design/PLAN.md:10` "Put any required engine-track change in
  `docs/design/REQUESTS_FOR_CLAUDE.md`."
- `docs/design/NIGHT_LOG.md:114` "See `REQUESTS_FOR_CLAUDE.md`:" followed by
  three inline numbered cross-track items.

Constraints on that fix: the replacement must not name a Claude-named artefact,
because this repository is public and carries a standing no-attribution rule.
It must not assert the three inline items in NIGHT_LOG are done or not done,
because that was never measured. Line numbers above were true at the stop and
must be re-read before editing.

## Order of work on resume

1. DONE. Both pointers now name `docs/design/REQUESTS_FROM_DESIGN.md`. A
   repo-wide grep over `*.md *.py *.js *.mjs *.cjs *.json *.txt` for either
   removed name returns nothing.
2. DONE. `python scripts/qa_citations.py` still exits 0 with the same figures:
   168 citations across 107 documents, 0 unresolvable, 0 needing review, 44
   waived. Both edits replaced text in place, so no line numbers moved.
3. DONE. The path sweep still reports 195 distinct paths named in docs. Absent
   went from 6 to 5; the closed one is the file fixed in step 1. The remaining
   5 absent and 3 untracked-but-present were each read and are correct
   statements or gitignored artefacts.
4. Commit every modified file, plus this one, with a conventional-commit
   message and no co-author trailer of any kind. Committing this file makes it
   tracked, so re-run `python scripts/qa_citations.py` afterwards: the two
   `file:line` citations in the section above only come under the gate once the
   file is tracked.
5. Two consecutive passes that surface nothing new are still outstanding.
   Passes 19 through 28 were all productive, so none of them count toward it.
6. Write the final report in the mandated shape: branch and commit, changes
   grouped by product, MCP, voice, UI, lessons, dissertation, media; the exact
   verification commands and their results; the demo rehearsal outcome and
   capture filenames; remaining limitations stated plainly; and the next
   irreversible decision, which is the owner's to make and not mine.

## Still owner-only, unchanged

Confirm the video duration, whether a slide deck and short report are also
required and whether submission is one ZIP, which page count the brief means,
and the submission route. Decide the `kodro` console-script entry point before
capture freeze. Tag and archive a submission commit. The public-history rewrite
for the six leaked compile logs. Manual desktop visual inspection. None of
these may be done without an explicit instruction naming the exact action and
target.
