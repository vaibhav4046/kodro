# Kodro HANDOFF (context pack for the next coding model)

Read this + STATE.md + BACKLOG.json + `git status` FIRST. Verify with one focused
command before trusting any count. Do not repeat broad audits if source is unchanged.

## What Kodro is
Offline-first robotics learning studio, ages 6-17. Design a robot from parts ->
configure hardware -> code (Blocks / Python subset / AI Vibe) -> deterministic sim
(2.5D + real 3D Three.js) -> inspect telemetry -> learn (KS1-KS4 lessons + grader)
-> validate -> export. Local Ollama optional (never mandatory). Also the artefact
for an MSc dissertation (COMP702, due 11 Sep 2026, docs/dissertation/*.tex -> .pdf).

## Architecture (verify in source)
- Web app: NO npm/build tooling. Vendored React + Three.js r137 UMD. ~36 IIFE
  modules in src/robolearn/assets/web/*.jsx|*.js concatenated by scripts/build_web.cjs
  (fixed ORDER array) into bundle.js. Rebuild+commit bundle.js after any edit;
  CI freshness gate = build_web.cjs --check. Static site: build_web.cjs --static.
- Modules talk via window.Kodro* globals: KodroAI, KodroProviders, KodroMemory,
  KodroMemoryGraph, KodroChatIntent, KodroCommands, KodroLessonGrader, RobotLab,
  KodroPhysics, KodroHooks, KodroPanels, KodroWorldFX.
- Desktop: pywebview bridge src/robolearn/web/app.py -> Kodro.exe (robolearn-web.spec,
  PyInstaller). Python pkg src/robolearn (grader, interpreter, engine, memory,
  ai/ollama_client.py). cap.html test harness GENERATED by build_screenshot_harness.cjs.
- CI: .github/workflows/ci.yml (ubuntu+macos+windows: ruff, ruff format, mypy,
  bundle freshness, web gates, qa_web on ubuntu, pytest). deploy-pages.yml on CI
  success. qa_ui + qa_worlds are LOCAL-only gates (Chrome + :8099 server).

## Gate commands (rerun; do not quote)
  The counts in brackets below are stale as of 2026-08-15 and are kept only as a
  rough shape of each gate. Measured live that day: qa_interpreter 180, not 157;
  pytest 1638 passed 1 skipped at 90.9 percent, not 1024. The others were not
  re-measured. pytest was re-run on 2026-08-17 at `e70b98b` on a clean tree:
  1641 collected, 1641 passed, 0 skipped, 90.85 percent. A run with 0 skipped is
  not evidence the Tk intermittency is fixed; this host has produced 1, then 2,
  then 0. Regenerate the artefact with `scripts/gen_test_suite_json.py`, never by
  hand. Rerun, never quote. pytest needs `--basetemp=<fresh dir>` on this
  host, see `.kodro/ca2-evidence/2026-08-15-suite-reproduction-and-tempdir-defect.md`.
  node scripts/qa_interpreter.mjs  (157) | qa_grader.mjs (34) | qa_physics.mjs (20)
  qa_ai_web.mjs (19) | qa_web.mjs (4/4, Chrome) | qa_worlds.mjs (61, :8099+Chrome)
  qa_ui.mjs (6/6+behaviour+12/12, :8099+Chrome) | python -m pytest -q (1024, >=85% cov)
  Serve for browser gates: cd src/robolearn/assets/web && python -m http.server 8099
  CHROME_PATH=/c/Program Files/Google/Chrome/Application/chrome.exe

## Hard constraints (never violate)
1. Honesty is the product. No fabricated tests/benchmarks/"10/10". Adversarial
   panel plateaus ~8.1; a literal 10 is not reachable without gaming. Report real.
2. No em/en dashes in dissertation/README/launch copy.
3. Offline-first, $0 default. Cloud opt-in BYOK only; keys in localStorage, sent
   only to the chosen provider, never logged. Never type the user's real keys.
4. Personas/evals are SIMULATED, label as such. Do not contradict submitted CA1.
5. Git: kodro-identity-pass -> ff main -> push both. NO Co-Authored-By trailer.
   ZERO Claude/Anthropic attribution in vaibhav4046 repos (global no-claude hook).
6. All gates green each commit; deploy gated on CI. Rebuild+commit bundle.js after
   web edits. Gate every behavioural change with a real test; verify in a real
   browser (headless Chrome via cap.html), not just compile.

## Shipped recently (build on, do not redo)
5 demo bugs; durable Vibe chat (persist); memory graph; chat-builds-world
(KodroChatIntent, conservative, offline); classroom learning loop (goal checklist,
progressive hints, ask-why, stuck-detect, next-lesson); round-2 direction/motion +
12 audited honesty bugs; light-theme HUD tokens (--hud-fg-*); Ollama auto-start +
warm on desktop launch (ollama_client.ensure_server) + persisted OLLAMA_ORIGINS.
