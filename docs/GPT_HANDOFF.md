# KODRO HANDOFF FOR GPT: WRITE THE FINAL MASTER PROMPT

You are GPT 5.x. Read everything below, then output ONE copy-paste master prompt for Claude Fable 5 running in Claude Code with ultracode enabled. Your output must be the prompt only, nothing else. The prompt you write drives one final, self-continuing engineering session that finishes the product AND keeps the dissertation submission-ready in parallel, with no further hard coding sessions needed afterwards.

## 1. WHAT KODRO IS (the aim your prompt must lock in)

Kodro is BOTH a testing platform and a learning platform, in one offline-first free app:

- TESTING: a person designs a customized robot from real parts within a budget, imports or enters real motor/battery/body numbers, and tests it in a simulated world BEFORE building it in the real world. They see real derived numbers (top speed, battery life, slope limit, stopping distance) and watch the build drive, crash, and drain in a living city. Multiple operations: Python code or block coding, seeded validation runs, replay, URDF exchange with ROS tools.
- LEARNING: the same app teaches robotics and programming through 18 graded lessons (ages 5-16), a class register for teachers, hints, and an AI helper that writes code from plain words.
- GOVERNANCE / HARNESS: a deterministic validator and sandbox always has the final word on what runs; the AI only proposes. Fidelity is disclosed per figure in three tiers (HONOURED / APPROXIMATED / NOT SIMULATED). Nothing leaves the user's machine by default; all AI options are free (local Ollama, or bring-your-own FREE key: Groq free tier, OpenRouter free models such as DeepSeek). Paid providers were deliberately removed.

Product rule number one: SIMPLIFY. Every change must make the product simpler and clearer for a first-time user, never more complicated. When in doubt, cut words, cut chrome, keep the world and the robot on screen.

## 2. CURRENT STATE (verified 2026-07-15, all numbers real)

- Live site: https://vaibhav4046.github.io/robolearn/ serving commit 291cf00 (live bundle sha256 verified equal to the committed build).
- Repo: github.com/vaibhav4046/robolearn. Local working copy: D:\project\robolearn. Branches: work on kodro-identity-pass, fast-forward main, push BOTH; deploy is gated on CI (3 OS matrix) via GitHub Pages.
- Desktop installer: PyInstaller spec robolearn-web.spec builds Kodro.exe (pywebview shell, about 187 MB, console off). The exe auto-starts and warms local Ollama via src/robolearn/ai/ollama_client.py ensure_server().
- Web app: no-build stack. Vendored React + Three.js UMD; 38 IIFE modules in src/robolearn/assets/web/*.jsx|*.js concatenated by scripts/build_web.cjs (fixed ORDER array) into a COMMITTED bundle.js. After ANY web edit: node scripts/build_web.cjs, commit bundle.js (CI freshness gate fails otherwise). cap.html is GENERATED by scripts/build_screenshot_harness.cjs (gitignored): edit the generator, never cap.html.
- Python package src/robolearn: engine (physics, terrain, sensors), interpreter mirror, grader, SQLite memory (pupils, concept EMA alpha=0.3), runtime sandbox + executor, envs/KodroEnv-v0 (gymnasium, optional rl extra), interop/urdf_io.py (URDF import AND export), kodrobench.py, bench.py.
- Shipped features (all gated): KRS spec import with energy-true grading; three-tier fidelity badges and verification report; URDF import/export; flagship Riverside City default world (buildings, parked cars, collidable trees, planters, lanes, LIVE pedestrian and traffic agents); 6 worlds + 17 mission sites (all still shipped; the dissertation claims them); runtime custom world (seed / density / traction); run reports with seeded deterministic Replay; memory graph + skill library; browser AND desktop pupil register; 24 lessons shown with plain age badges; blocks editor; 5-seed scenario validator; Realism dashboard; bounded grammar-constrained AI tool call (set_world); chat builds robots and switches worlds even with no AI present; views are 3D orbit and flat 2D only (first person removed); first-run render quality auto-picks from device memory/cores.

## 3. QUALITY GATES (the prompt must keep every one green at every commit)

Counts in this section are a snapshot from 28 July 2026, the date this file was
last written, and several have grown since. Treat the command names as current
and the numbers as historical. The current figure for every gate, with the exact
summary line it printed, is in
[`docs/eval/qa_gate_runs_2026-08-14.md`](eval/qa_gate_runs_2026-08-14.md).

Node gates (node scripts/<name>.mjs): qa_interpreter 180, qa_grader 34, qa_physics 20, qa_ai_web 27, qa_web 4, qa_parts 40, qa_memgraph 22, qa_pupilstore 23, qa_scenario_parity 4, qa_interp_fixes 13. Browser gates (serve src/robolearn/assets/web on :8099 with python -m http.server first): qa_ui (6 flows + 32 behaviour + 12 modals), qa_worlds 61. Ollama-gated: qa_ai_grammar 6. Python: python -m pytest = 1068 passed at 89 percent coverage (85 gate); ruff check; ruff format --check; mypy (66 files, strict; optional extras have overrides). CI runs on ubuntu + macos + windows; deploy fires only on CI success; after deploy, verify the LIVE bundle sha256 equals the committed one before claiming shipped.

Known traps to write into the prompt verbatim: the robolearn editable-install .pth must point at D:\project\robolearn\src; a backslash-n inside the harness generator template must be escaped double or cap.html breaks at parse; qa harness Chrome profiles are wiped per dump because a reused profile serves stale cached assets; qa_ui/qa_worlds can flake with chrome spawn timeouts under heavy load (rerun after cooldown; CI does not run them); always run ruff format, not only ruff check; OLLAMA_ORIGINS must include https://vaibhav4046.github.io for the live site to reach local Ollama.

Durable state lives in .kodro/autonomy/ (STATE.md, BACKLOG.json, HANDOFF.md, DECISIONS.md, SESSION_LOG.md). The prompt must require reading these first and updating them after every landed change, so ANY future session resumes seamlessly.

## 4. DISSERTATION (maintained in parallel by the same session)

- File: docs/dissertation/Kodro_Dissertation.tex -> Kodro_Dissertation.pdf. EXACTLY 50 pages (hard limit), ZERO em or en dashes anywhere, builds with MiKTeX pdflatex run twice. COMP702, University of Liverpool, student 201979723, supervisor Keith Dures, due 11 September 2026, Turnitin.
- CA1 real grade: B. Written criticisms and status: no BCS consideration (FIXED: Professional Issues chapter maps the BCS criteria); ethics not separate (FIXED: standalone Ethics section); use case and UI/UX clarity for I/O scenarios (partially addressed in product copy; the dissertation walkthrough can still improve); some prose reads AI-generated (style rule below).
- Style: organic, blunt, concrete, first person, short sentences, no cliches, no em dashes. Every number in the tex must match a measured gate result. Simulated persona evaluations stay labelled simulated. No human-study results exist and none may be implied. Never write any 10/10 or perfect-score claim.
- Rule: any product change the session makes that alters a number or feature the tex mentions must be reflected in the tex in the SAME loop iteration, and the PDF rebuilt and page-counted (stay at or under 50).

## 5. HARD CONSTRAINTS (non-negotiable, put them at the top of your prompt)

1. HONESTY IS THE PRODUCT. Never fabricate test results, benchmarks, user studies, or grades. Report failures with real output. A claim without a passing gate is not made.
2. Free and offline by default. No paid services, no accounts, no telemetry. BYOK keys are free-tier only (Groq, OpenRouter), live in localStorage, are sent only to the chosen provider, never logged. The offline-guard pytest enforces exactly these hosts.
3. Do not contradict the submitted CA1 or the current dissertation claims (17 sites, gate counts get updated not deleted).
4. Git: work on kodro-identity-pass, ff main, push both, CI green before deploy claims. No Co-Authored-By trailer. Zero Claude or Anthropic attribution anywhere in the repo (a global git hook enforces this).
5. Every behavioural change gets a real gate (unit case or headless-Chrome concern) in the same commit. Browser verification means the real bundle driven through the harness, not compile-passes.
6. Pupil data stays on device. No new network surface of any kind.
7. Simplify relentlessly; do not add features that add words to the first screen.

## 6. WHAT YOUR MASTER PROMPT MUST INSTRUCT (shape of the final session)

- One continuous, self-directing loop, NOT a phase list: measure the real product (run every gate, load the live site headlessly, screenshot key surfaces) -> convene an adversarial judge panel (parallel subagents: first-time user, teacher, robot builder on a budget, accessibility reviewer, performance reviewer, honesty auditor; every finding must survive an independent refutation pass before it counts) -> prioritise verified findings by user impact -> fix root causes -> add or update the gate -> run the full battery -> ship through CI to live and verify the live hash -> update .kodro/autonomy and the dissertation -> loop again. Convergence rule: the session ends only when TWO consecutive full judge rounds surface zero new verified defects and the dissertation builds clean at 50 pages; otherwise it keeps looping. If the session is interrupted for any reason, the durable state files make the next session continue exactly where it stopped, so the work is effectively continuous without pretending token limits do not exist.
- Design mandate inside the loop: one flagship world experience, cinematic 3D + flat 2D only, minimal words, use case stated in one sentence on first contact, learning path obvious (lessons one click away), testing path obvious (import or build, press Run, read the honest numbers). Redesign is allowed only where it simplifies.
- Budget-build story: strengthen the existing budget planner so a user can type a budget and get a real-parts plan tied to the simulated build (parts DB already cites Pololu with sources; never invent part numbers or prices without a cited source).
- QA testing by actual judge panel every round, as above, with results recorded in BACKLOG.json using the existing severity scheme (P0 data-loss/false-claim, P1 broken core flow, P2 misleading, P3 cosmetic).
- Dissertation upkeep every round as section 4.
- Final deliverable statement the prompt must demand: live URL serving the final commit (hash-verified), full gate matrix pasted as real output, dissertation PDF at 50 pages with zero dashes, updated .kodro/autonomy state, and an honest list of anything deliberately not done with reasons. No perfect-score language anywhere.

Write the master prompt now: direct, imperative, self-contained (it must not assume this document travels with it, so restate every path, number, constraint, and trap it needs), and safe (it must comply with the assistant's safety rules; do not ask the model to bypass anything, run forever literally, or fabricate). Output only the prompt.
