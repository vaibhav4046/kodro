# Classroom setup

Full content lands in Task 20 of the build plan. This page will cover:

- Recommended hardware (any machine that runs Python 3.12 — a 2018-era laptop
  is more than enough).
- Distributing the binary across a managed Windows estate.
- Where the local pupil database lives (`~/.robolearn/pupil.db`) and how to
  back it up.
- How to reach the teacher dashboard (`Ctrl+Shift+T`).
